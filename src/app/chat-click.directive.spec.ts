import { ChatClickDirective } from './chat-click.directive';

describe('ChatClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatClickDirective();
    expect(directive).toBeTruthy();
  });
});
